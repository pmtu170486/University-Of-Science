<Page
    x:Class="demoBinding.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:demoBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
            <ListView Name="lstView" Grid.Column="1" 
                      HorizontalAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding name}"
                           FontSize="35"/>
                            <TextBlock Text="{Binding phone}"
                        FontSize="20"/>
                            <TextBlock Text="{Binding email}"
                           FontSize="20" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="20">
                <TextBlock Text="Name: " FontSize="30"/>
                <TextBox Name="txtName" Width="400"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="20">
                <TextBlock Text="Phone: " FontSize="30"/>
                <TextBox Name="txtPhone" Width="400"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="20">
                <TextBlock Text="Email: " FontSize="30"/>
                <TextBox Name="txtEmail" Width="400"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" 
                        Margin="20">
                <Button x:ConnectionId='1' Content="Add" Name="btnAdd" 
                                            />
            </StackPanel>

        </StackPanel>
    </Grid>
</Page>

